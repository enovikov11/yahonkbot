Таймлайн разработки: каждый шаг на час разработки с понятным критерием готовности

Разработка:
  Бот на typescript и deno:
    Парсилка конфигов, которая выдает из json типизированные данные или падает, учитывая временные 8h например: +тесты
    При апдейте показывать diff с прошлым:
    API yahoo:
    API telegram: +тесты интеграционные
      generator который кидает апдейты если совпадает chatId:
    API debounce, get variables:
    utils:
      по апдейту определяет isPremium, commandType, payload(для смены названия):
    titles:
      матчилка переменных:
    Статистика:
      часто используемые команды включая пустышки:
      хонкеры недели (настраиваемое):
      менятели названия, недели, ever:
      ссылки на историю названий:
    Отвечалка на все сообщения onUpdate:
      Процессилка:
        Уметь определять непосредственно команду:
      Удалялка изменений title ботом:
      Сохранялка в базу honk, title:
    ads рассылка:
    sqlite:
  Микросервис картинок title, nodejs puppeteer и без логики:
    починить firstname null:
  Микросервис видео с полосками и историчностью, nodejs puppeteer и без логики:
Деплой:
  Cмигрировать логи в базу, стереть избыточные данные:
  Передеплоить инстанс, ротировать ключи:
  Для каждого чата свой бот:
Держать минус ДЦ, postgresql: